bundle:
  name: retail_lakehouse

include:
  - resources/**/*.yml

sync:
  include:
    - src/**
    - config/**
  exclude:
    - .venv/**
    - .databricks/**
    - .vscode/**
    - tests/**
    - "**/__pycache__/**"

targets:

  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405604563582301.1.azuredatabricks.net
      root_path: /Workspace/Users/sanketazure19@gmail.com/.bundle/${bundle.name}/${bundle.target}

  staging:
    mode: development
    workspace:
      host: https://adb-7405604563582301.1.azuredatabricks.net
      root_path: /Workspace/Users/sanketazure19@gmail.com/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      host: https://adb-7405604563582301.1.azuredatabricks.net
      root_path: /Workspace/Users/sanketazure19@gmail.com/.bundle/${bundle.name}/${bundle.target}

    resources:
      jobs:
        retail_lakehouse_pipeline:
          schedule:
            quartz_cron_expression: "0 50 23 * * ?"
            timezone_id: "Asia/Kolkata"
            pause_status: UNPAUSED