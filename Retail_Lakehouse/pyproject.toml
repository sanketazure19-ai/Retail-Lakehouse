[project]
name = "retail-lakehouse"
version = "0.0.1"
authors = [{ name = "sanketazure19@gmail.com" }]
requires-python = ">=3.10,<3.13"
dependencies = [
    "pyyaml",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "databricks-dlt",
    "databricks-connect>=15.4,<15.5",
    "ipykernel",
    "build",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/retail_lakehouse"]

[tool.hatch.build.targets.wheel.force-include]
"config/environments.yml" = "retail_lakehouse/config_data/environments.yml"
"config/datasets.yml" = "retail_lakehouse/config_data/datasets.yml"

[tool.ruff]
line-length = 120