resources:
  apps:
    retail_lakehouse_qa:
      name: retail-lakehouse-qa
      description: "GlobalMart Retail Lakehouse QA automation dashboard"
      source_code_path: ../../app

      resources:
        - name: qa-job
          job:
            id: ${resources.jobs.qa_automation.id}
            permission: CAN_MANAGE_RUN
