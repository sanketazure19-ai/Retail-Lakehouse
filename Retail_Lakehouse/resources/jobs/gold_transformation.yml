resources:
  jobs:
    gold_transformation:
      name: "[${bundle.target}] Retail Lakehouse - Gold Transformation"

      environments:
        - environment_key: default
          spec:
            client: "4"
            dependencies:
              - ../../dist/*.whl

      tasks:

        - task_key: gold_transformation
          notebook_task:
            notebook_path: ../../src/notebooks/03_gold_transformation.py
            base_parameters:
              environment: ${bundle.target}
          environment_key: default